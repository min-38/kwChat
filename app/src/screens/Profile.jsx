const { SafeAreaView, Text } = require("react-native");

function ProfileScreen() {
    return (
        <SafeAreaView>
            <Text>Profile</Text>
        </SafeAreaView>
    )
}

export default ProfileScreen