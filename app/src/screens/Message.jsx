const { SafeAreaView, Text } = require("react-native");

function MessageScreen() {
    return (
        <SafeAreaView>
            <Text>Message</Text>
        </SafeAreaView>
    )
}

export default MessageScreen