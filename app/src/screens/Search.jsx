const { SafeAreaView, Text } = require("react-native");

function SearchScreen() {
    return (
        <SafeAreaView>
            <Text>Search</Text>
        </SafeAreaView>
    )
}

export default SearchScreen